#ifndef HASH_MAP_HPP
#define HASH_MAP_HPP

#include <array>
template <typename T, typename K, size_t N>
class HashMap {
	
	std::array<T, N> m_table;
	std::array<K, N> m_keys;

public:


	
};
#endif // !HASH_MAP_HPP